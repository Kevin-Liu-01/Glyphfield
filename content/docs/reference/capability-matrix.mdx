---
title: Capability matrix
description: Compare Studio tools, browser downloads, and agent API equivalents.
icon: TableProperties
---

| Capability | Browser output | Agent API |
| --- | --- | --- |
| Brand settings | Persistent identity | Identity object in every request |
| Brand elements | JSON brief | `element-brief` JSON |
| Moodboard | PNG, identity JSON | Browser only |
| Animation | GIF from text, image, and logo sequences with live per-frame materials | Browser only |
| Lottie | Edited JSON or `.lottie`, high-resolution frame PNG composited over solid, transparent, or live-material backgrounds | Browser only |
| Material | PNG and animated GIF | Browser only |
| Surface Lab | PNG; static background SVG through generation API | `background` SVG for deterministic recipes |
| OpenGraph | PNG | Compose with template/background SVG |
| Color tokens | Copied CSS tokens | Identity HEX fields |
| Typography | Browser preview, local font | Documented font stack only |
| Terminal card | PNG | Browser only |
| Partnership | PNG | `template: partnership` SVG |
| Blog cover | PNG | `template: blog` SVG |
| Slide templates | PNG | `template: slides` SVG |
| Component library | Interactive preview | Element briefs for product patterns |

## Shared editor capabilities

| System | Browser behavior | Persistence and output |
| --- | --- | --- |
| Color control | Native picker, HEX, OKLCH, optional opacity | Controlled value shared by previews and exports |
| Canvas viewport | Zoom from 40–200 percent, reset, and fit | Zoom saved per project and tool; intrinsic artifact ratio is unchanged |
| Editable layers | Select, drag, resize, keyboard nudge | Transform values feed the same renderer used for export |
| Layer panel | Reorder, six-way alignment, reset, boundary-disabled actions | Layer order and transforms saved per project and template |
| Material finishes | Reflections, shadows, borders, glass, and presets | Finish object travels with animation and shader settings |
| Uploaded assets | Logos, fonts, backgrounds, partner marks, Lottie JSON, and `.lottie` bundles | Sources remain local; supported exports embed resolved visual frames |

## Browser-only reasons

Browser-only features depend on one or more of Canvas, WebGL, local font loading, object URLs, interactive visual placement, or multi-frame capture.

## API strengths

The API is best for repeatability, source-controlled SVG, batch generation, agent workflows, and downstream rendering pipelines.
